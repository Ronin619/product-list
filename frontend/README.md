## Getting Started

1. While in the frontend directory, enter "npm install" in the terminal.
2. Enter "npm run dev" to start the server.

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.
